apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "strimzi-kafka.fullname" . }}-test-connection"
  labels:
    {{- include "strimzi-kafka.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "strimzi-kafka.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
