# Notes

Quick mental model to lock these in

NeedServiceConnect a couple VPCsVPC PeeringConnect many VPCs / on-prem, centrallyTransit GatewayPrivate, dedicated line to on-premDirect ConnectEncrypted tunnel over internet to on-premSite-to-Site VPNPrivate access to S3/DynamoDB from within VPCGateway EndpointPrivate access to other AWS services from within VPCInterface Endpoint (PrivateLink)